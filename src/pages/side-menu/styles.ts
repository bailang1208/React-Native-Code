import styled from 'styled-components/native';

const Wrapper = styled.ScrollView`
  flex: 1;
  padding-top: 50px;
`;
const TopWrapper = styled.View`
  flex: 1;
  align-items: center;
`;
const MenuItemsWrap = styled.View`
  padding-horizontal: 50px;
  padding-top: 20px;
`;

export {Wrapper, TopWrapper, MenuItemsWrap};
